local config = {
    wibar = {
        enabled = false,
        show_current_layout_indicator = false
    },
    client = {
        titlebar = {
            enabled = false
        }
    }
}

return config
